name: Validate tutorials

on:
  push:
  pull_request:

jobs:
  tutorial-validation:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake gfortran libopenmpi-dev openmpi-bin

      - name: Build external libraries
        run: FC=gfortran CC=gcc BUILD_JOBS=2 ./external/CMake_build.sh

      - name: Run tutorial validation gate
        run: python3 tools/validate_tutorials.py --ranks 2

      - name: Run tutorial gate with GNU runtime checks
        run: |
          make clean
          make GFORTRAN_EXTRA_FFLAGS="-O0 -fcheck=all -fbacktrace -ffpe-trap=invalid,zero,overflow"
          python3 tools/validate_tutorials.py --skip-build --ranks 2
